<h1>カード検索</h1>

<hr>
<div class="serach_form">
  <%= form_for(@search, url: search_cards_path, method: :post) do |f| %>
    <div>
      <p>エキスパンション</p>
      <%= f.select :expansion, Expansion.all.order(:released_at).map{|e| [e.abbr, e.name]} %>
    </div>
    <div>
      <p>カードタイプ</p>
      <%= f.text_field :types %>
    </div>
    <div>
      <%= f.submit "検索", class:'btn btn-sm btn-primary' %>
    </div>
  <% end %>
</div>
